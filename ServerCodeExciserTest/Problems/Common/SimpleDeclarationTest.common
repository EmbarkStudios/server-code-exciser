class USimpleDeclarationTest
{
	void Test()
	{
		int SomethingBefore = 0;
		SomethingBefore++;

		ensure(System::IsServer());

		float ThisMustBeGuarded = 0.0f;
		ThisMustBeGuarded--;

		while(true)
		{
			FString ThisAsWell = "Yep";
		}

		ThisMustBeGuarded++;
	}
};
